import styled, { css } from 'styled-components';import { ReactComponent as UserInputSvgIcon } from '../../assets/icon/user-01.svg';import { COLORS, SPACES } from '../../theme';import { Button } from '../common/component/button';import { CloseAndOther } from '../common/component/close-and-other';export const Container = styled.div`  display: flex;  flex-direction: row;`;const buttonsSharedStyels = css`  padding-top: ${SPACES.xxs};  padding-bottom: ${SPACES.xxs};`;export const UserInputIcon = styled(UserInputSvgIcon)`  color: ${COLORS.halfTransparentGrey};`;export const SaveButton = styled(Button)<{ hasErrors?: boolean }>`  ${buttonsSharedStyels}  ${({ hasErrors }) =>    hasErrors &&    css`      margin-bottom: 0 !important;    `}`;